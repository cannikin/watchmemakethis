= render :partial => 'form', :locals => { :build => @new_build, 
                                          :submit_path => create_build_path, 
                                          :button_label => 'Create Build!',
                                          :show_delete => false }

= content_for :body_end do
  :javascript
    WatchMeMakeThis.autoFillFields($('#build_name'), [$('#build_path'), $('#build_hashtag')]);
    