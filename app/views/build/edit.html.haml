= render :partial => 'form', :locals => { :build => @edit_build, 
                                          :submit_path => update_build_path, 
                                          :button_label => 'Save Changes',
                                          :show_delete => true }
  