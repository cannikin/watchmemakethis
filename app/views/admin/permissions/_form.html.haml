= semantic_form_for [:admin,@permission] do |form|
  = form.inputs do
    = form.input :name, :label => 'Name'
    = form.input :description, :label => 'Description'
    = form.input :roles, :label => 'Roles'
  = form.buttons
